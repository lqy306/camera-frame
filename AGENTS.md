# Camera Frame PWA 维护约束

1. 本仓库是独立 PWA，不得假设或修改仓库外的桌面版项目。
2. 不得上传、遥测、记录或持久化用户照片；所有处理只在当前浏览器本地完成。
3. 不得使用远程 CDN、远程字体或运行期第三方接口，生产构建必须可完全离线运行。
4. 固定相机主体只能来自 `camera_rgba.png`，相机机身像素不得被背景综合色污染。
5. 默认背景模式必须为 `solid`（纯色全替换），默认颜色强度必须为 100%。
6. 液晶屏和下方照片默认使用同一构图；只有用户关闭同步后才可分别编辑。
7. 每次修改后必须运行 `npm run test` 和 `npm run build`。
8. HTML、JS、CSS、Worker、相机模板、蒙版、阴影、图标与配置必须全部进入 Service Worker 离线缓存。
